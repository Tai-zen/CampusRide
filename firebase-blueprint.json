{
  "entities": {
    "UserProfile": {
      "title": "UserProfile",
      "description": "Represents a unified user profile for students, drivers, or admins.",
      "type": "object",
      "properties": {
        "id": { "type": "string", "description": "The unique user identifier matching the Auth UID." },
        "name": { "type": "string", "maxLength": 100 },
        "email": { "type": "string", "format": "email" },
        "role": { "type": "string", "enum": ["student", "driver", "admin"] },
        "avatar": { "type": "string" },
        "idNumber": { "type": "string", "maxLength": 50 },
        "enrolledTerm": { "type": "string", "maxLength": 100 },
        "major": { "type": "string", "maxLength": 100 },
        "walletBalance": { "type": "number" },
        "isVerified": { "type": "boolean" },
        "tripsThisWeek": { "type": "number" },
        "carpoolRides": { "type": "number" },
        "savedThisMonth": { "type": "number" },
        "vehicle": { "type": "string", "maxLength": 200 },
        "rating": { "type": "number" },
        "ratingsCount": { "type": "number" },
        "todayEarnings": { "type": "number" },
        "completedTripsCount": { "type": "number" },
        "hoursOnline": { "type": "number" },
        "status": { "type": "string", "enum": ["On Trip", "Idle", "Break", "Offline"] },
        "createdAt": { "type": "string" },
        "adminComments": { "type": "string", "maxLength": 500 },
        "declined": { "type": "boolean" },
        "declineReason": { "type": "string", "maxLength": 500 }
      },
      "required": ["id", "name", "email", "role"]
    },
    "RideRequest": {
      "title": "RideRequest",
      "description": "Represents a physical ride booking request.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "passengerId": { "type": "string" },
        "passengerName": { "type": "string" },
        "passengerAvatar": { "type": "string" },
        "passengerRating": { "type": "number" },
        "passengerType": { "type": "string", "enum": ["Student", "Staff", "Faculty"] },
        "pickup": { "type": "string" },
        "dropoff": { "type": "string" },
        "status": { "type": "string", "enum": ["requested", "accepted", "arriving", "in_transit", "completed", "canceled"] },
        "vehicleType": { "type": "string", "enum": ["Car", "Keke", "Shuttle"] },
        "verifyPeer": { "type": "boolean" },
        "driverId": { "type": "string" },
        "driverName": { "type": "string" },
        "driverAvatar": { "type": "string" },
        "driverVehicle": { "type": "string" },
        "driverRating": { "type": "number" },
        "cost": { "type": "number" },
        "etaMinutes": { "type": "number" },
        "date": { "type": "string" },
        "time": { "type": "string" },
        "createdAt": { "type": "number" },
        "paymentMethod": { "type": "string", "enum": ["cash", "transfer"] },
        "paymentConfirmedByRider": { "type": "boolean" },
        "paymentValidatedByDriver": { "type": "boolean" },
        "riderPaid": { "type": "boolean" }
      },
      "required": ["id", "passengerId", "pickup", "dropoff", "status", "cost"]
    },
    "Transaction": {
      "title": "Transaction",
      "description": "A ledger record of student wallet reload or ride fare payment.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "description": { "type": "string", "maxLength": 200 },
        "amount": { "type": "number" },
        "date": { "type": "string" },
        "time": { "type": "string" },
        "method": { "type": "string" },
        "type": { "type": "string", "enum": ["charge", "reload"] },
        "status": { "type": "string", "enum": ["Completed", "Pending", "Canceled"] }
      },
      "required": ["id", "description", "amount", "type", "status"]
    },
    "Notification": {
      "title": "Notification",
      "description": "An inbox alert message.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "title": { "type": "string", "maxLength": 100 },
        "message": { "type": "string", "maxLength": 500 },
        "time": { "type": "string" },
        "date": { "type": "string" },
        "isRead": { "type": "boolean" },
        "type": { "type": "string", "enum": ["info", "success", "receipt", "alert"] }
      },
      "required": ["id", "title", "message", "isRead", "type"]
    },
    "PendingDriver": {
      "title": "PendingDriver",
      "description": "Driver signup details awaiting admin approval.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "carBrand": { "type": "string" },
        "carType": { "type": "string" },
        "plateNumber": { "type": "string" },
        "vehicleId": { "type": "string" },
        "createdAt": { "type": "string" }
      },
      "required": ["id", "name", "email"]
    },
    "SentEmail": {
      "title": "SentEmail",
      "description": "Log of email dispatch alerts sent by administration action.",
      "type": "object",
      "properties": {
        "to": { "type": "string" },
        "name": { "type": "string" },
        "subject": { "type": "string" },
        "type": { "type": "string", "enum": ["approve", "decline"] },
        "comments": { "type": "string", "maxLength": 500 },
        "dateSent": { "type": "string" },
        "vehicle": { "type": "string" },
        "password": { "type": "string" }
      },
      "required": ["to", "name", "subject", "type", "dateSent"]
    },
    "Review": {
      "title": "Review",
      "description": "Represents a passenger review for a driver.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "passengerName": { "type": "string", "maxLength": 100 },
        "passengerAvatar": { "type": "string" },
        "rating": { "type": "number" },
        "comment": { "type": "string", "maxLength": 500 },
        "time": { "type": "string" },
        "date": { "type": "string" },
        "createdAt": { "type": "number" }
      },
      "required": ["id", "passengerName", "rating", "comment", "createdAt"]
    },
    "School": {
      "title": "School",
      "description": "Custom configuration metadata for universities, such as branding logos.",
      "type": "object",
      "properties": {
        "logoImage": { "type": "string", "maxLength": 1000 },
        "updatedAt": { "type": "number" }
      },
      "required": ["logoImage"]
    }
  },
  "firestore": {
    "/schools/{schoolId}": {
      "schema": "School",
      "description": "Custom configuration metadata and branding for a specific university."
    },
    "/users/{userId}": {
      "schema": "UserProfile",
      "description": "Root user profile storage."
    },
    "/users/{userId}/transactions/{transactionId}": {
      "schema": "Transaction",
      "description": "Nested transaction history for a specific user."
    },
    "/users/{userId}/notifications/{notificationId}": {
      "schema": "Notification",
      "description": "Nested notifications inbox for a specific user."
    },
    "/users/{userId}/reviews/{reviewId}": {
      "schema": "Review",
      "description": "Nested passenger reviews for a specific driver."
    },
    "/rideRequests/{rideId}": {
      "schema": "RideRequest",
      "description": "Public ride request listings."
    },
    "/pendingDrivers/{driverId}": {
      "schema": "PendingDriver",
      "description": "Pending driver applications."
    },
    "/sent_emails/{emailId}": {
      "schema": "SentEmail",
      "description": "Administrative email action dispatch history logs."
    }
  }
}
